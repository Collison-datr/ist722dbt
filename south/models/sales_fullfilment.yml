
models:
  - name: sales_fulfillment
    description: The sales fact table combined with all of its outrigger dimensions into one bit table. Transaction fact table with one row per order does include product.
